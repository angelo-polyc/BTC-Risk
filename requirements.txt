# Full dependency set for the BTC drawdown-probability model + ops layer.
#
# Installed at Replit deploy time via `.replit`'s build phase, not at
# pipeline-run time. This replaces the "pip install" step that was in
# run_all.sh — scheduled daily runs should use `--skip-deps` to bypass
# that step since everything is pre-installed here.
#
# Split into two groups for readability; pip treats them identically.

# ─── Model pipeline deps (from run_all.sh's pip install step) ─────────────
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyarrow>=14.0
yfinance>=0.2
fredapi>=0.5
velodata>=1.0
cot_reports>=0.1
artemis>=0.1
requests>=2.31

# ─── Database ─────────────────────────────────────────────────────────────
psycopg2-binary>=2.9

# ─── Ops layer (scheduler, REST API, MCP server) ──────────────────────────
apscheduler>=3.10
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
mcp[cli]>=1.0
